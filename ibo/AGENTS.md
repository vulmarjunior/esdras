<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# ESDRAS — Reforma do Estatuto da IBO

Aplicação web para a **Comissão de Reforma do Estatuto Social da Igreja Batista Olaria (IBO)** (9 membros): análise do Estatuto dispositivo por dispositivo, sugestões, registros de reuniões e atas, e geração da proposta consolidada. A especificação completa está em `../PRD_ESDRAS.md` (raiz do repositório), complementada pelas decisões registradas neste arquivo.

## Estado do projeto (última atualização)

- MVP completo em **Next.js 16 + Supabase Postgres** (via `pg`, connection string do shared pooler). Deploy alvo: Vercel. Toda a IA é via Groq API, chamada exclusivamente no servidor.
- **Banco**: Postgres (Supabase). `DATABASE_URL` no `.env.local` (pooler Session, porta 5432). A camada `lib/db.ts` converte `?`→`$N`, `datetime('now')`→`to_char(now(),...)` e `INSERT OR IGNORE`→`ON CONFLICT DO NOTHING`. Dados carregados via `scripts/migrate-to-pg.mjs` (idempotente: drop + recria + copia do SQLite). `lib/schema.sql` é a referência de schema; datas ficam como TEXT no mesmo formato do SQLite.
- **Conexões serverless**: mesmo que `DATABASE_URL` ainda esteja cadastrada com a porta 5432, `lib/db.ts` converte URLs do shared pooler do Supabase para a porta 6543 (transaction pooling) em runtime. O pool `pg` usa `max: 1` por padrão (`DB_POOL_MAX` permite ajuste), `idleTimeoutMillis: 5000` e transações isoladas por `AsyncLocalStorage`. Essa configuração corrige `EMAXCONNSESSION` no Vercel; não restaurar pool de sessão para o runtime serverless.
- Autenticação local: JWT (jose) + bcrypt, sessão em cookie `esdras_session`. **Troca de senha obrigatória no primeiro acesso**: `must_change_password` (claim `mc` no JWT; `proxy.ts` + layout + `requireUser` bloqueiam até trocar; página `/trocar-senha`). Usuários criados pelo admin e senha redefinida pelo admin reativam a obrigação. Usuários de demonstração: `admin@ibo.local`/`admin123`, `coordenador@ibo.local`/`coord123`, `membro1@ibo.local`…`membro7@ibo.local`/`membro123` (trocam a senha no 1º login). Telefone em `users.phone` com link WhatsApp (`lib/phone.ts`: `normalizarTelefone`/`formatarTelefone`/`whatsappLink`) — exibido no Admin e na presença das reuniões.
- **Últimas features entregues**: migração Supabase (`pg`, async), CRUD de reuniões/dispositivos/usuários, editor de texto rico (zero-dep) com sanitização, renumeração (§17 simulador + aplicar + **reordenação física de artigos** via `moveProvision`/`lib/reorder-core.ts`), referências cruzadas (§18), votação de sugestões, aprovar dispositivo na reunião (§23), retificação de ata (§29), coerência IA (§33), paginação de auditoria, perfis centralizados (`lib/permissions.ts`), troca de senha obrigatória no 1º acesso, telefone com link WhatsApp (`lib/phone.ts`), realtime (§40 Broadcast + Presence via `lib/realtime.ts`), **tela do dispositivo em abas** (`components/provision/device-tabs.tsx`), **anotação pessoal por membro** (`personal_notes` — privada, no topo da aba Colaboração; `app/actions/notes.ts` + `components/provision/personal-note-form.tsx`), **guia de redação (LC 95/1998 + Manual de Redação da Presidência)** — `lib/legal-refs.ts` curado, prompts de IA enriquecidos, `action: duvida` e `action: valida_tecnica` (checklist LC 95 no editor), página `/guia-redacao` (`components/guia/*`), testes Vitest (50), **biblioteca doutrinária (`/documentos`)** — 6 documentos confessionais batistas (Londres 1689, New Hampshire 1833, Fé e Mensagem 2000, Declaração Doutrinária da CBB, Princípios Batistas, Pacto das Igrejas Batistas; **Filadélfia 1742 removida por decisão do usuário**; Declaração de Fé da Igreja Local fica fora — outro projeto), dados gerados por `scripts/build-confissoes.mjs` em `lib/confissoes/data/*.json` a partir de `../Documentos fonte`, leitura por seções + busca em `components/documentos/biblioteca.tsx`, consulta IA (`action: consulta_doutrinaria` no `/api/ai`, injeta trechos via `lib/confissoes/recuperacao.ts` `buscarTrechos`), testes Vitest (66), **indicador de anotação pessoal** — badge violeta "nota" (`NotedBadge` em `lib/status-badge`) no painel (`components/dashboard/dashboard-tree.tsx`), na navegação estrutural (`components/structural-nav.tsx`) e no cabeçalho do dispositivo, com filtro "Somente com minhas anotações" (`lib/dashboard-filter.ts`; `getPersonalNoteIds` em `lib/data.ts`), testes Vitest (72), **manual, onboarding e ajuda** — `lib/manual.ts` (16 seções do manual, baseadas no PRD e nas telas) + página `/manual` (`components/manual/*`), assistente de ajuda (`action: ajuda` no `/api/ai`), botão flutuante "Ajuda" (`components/help/help-fab.tsx` + `help-assistant.tsx`), modal de boas-vindas (`components/onboarding/welcome-modal.tsx`) e card "Primeiros passos" no painel (`components/onboarding/first-steps.tsx`, estado em localStorage por usuário), testes Vitest (77).
- **PWA + responsividade** — o app é **PWA instalável**: `app/manifest.ts` (manifest + ícones PNG gerados por `scripts/generate-icons.mjs` a partir de `public/icon.svg`), service worker `public/sw.js` (rede-primeiro para navegações — evita servir conteúdo autenticado velho; cache-primeiro para estáticos; nunca cacheia `/api/*`; fallback `public/offline.html`), registro via `components/pwa-register.tsx` (só produção), `viewport`/`appleWebApp`/tema no layout raiz. Responsividade auditada (PRD §42): layout fluido, menu mobile em Sheet, grids `sm/lg`, tabelas do simulador/reordenação com `overflow-x-auto`; `input/textarea/select` com fonte ≥16px no mobile (evita zoom do iOS).
- Veja `PENDENCIAS.md` para a pauta de trabalho do próximo agente.
- Antes de alterar a Mesa, o Acompanhamento, a navegação por perfil ou o fluxo editorial, leia `docs/registro-reformulacao-mesa-2026-09-22.md`. O documento registra as decisões do usuário, compatibilidades, arquivos centrais, situação do banco compartilhado entre preview/produção e limites para mudanças futuras.

- **Estatuto em revisão (`/revisao`)** — leitura integral emparelhada com o vigente, incluindo não aprovados; busca, capítulos, filtros, diff por palavras, marcas de conteúdo separadas do status. `lib/review-core.ts` monta os pares na ordem atual; texto atual: trabalho → proposta inicial → vigente (ignora HTML vazio). `lib/review-original.ts` preserva numeração/título/parentesco/ordem da importação via JSONs por ID; o texto anterior usa `texto_vigente` para respeitar correções de extração. Não substituir metadados históricos pelos números atuais; identidades desconhecidas mostram numeração indisponível. Sem migração de banco. Revogados permanecem visíveis como retirada indicada; filhos têm análise independente. Menu Reforma, proxy protegido e manual atualizados. A página segue a **versão de trabalho** (Proposta/Vigente) escolhida no painel.
- **Trabalho na proposta (numeração, ordem e referências)** — o painel, a navegação do dispositivo e `/revisao` exibem a versão escolhida num cookie `esdras_versao` (padrão **proposta**; `lib/versao.ts` + `components/version-toggle.tsx`). Na Proposta, o número exibido é o **do documento original** (placements), com chip `era X` (vigente) e aviso `ordem: Y` quando a ordem atual sugerir outro número (`lib/numeracao.ts` deriva a numeração sequencial: artigos 1..N, capítulos em romanos, revogados não ocupam número). `/renumeracao` ganhou **Ordenar pela numeração do documento** (reordena por capítulo; `ordenarPorNumeroDocumentoAction`), relatório de capítulos e "Aplicar numeração" que grava artigos+capítulos e cria **pendências automáticas** (`referencia_cruzada`) para artigos aprovados que mudarem de número; `moveProposalProvision` faz o mesmo ao mover aprovados. Referências internas: `lib/referencias-core.ts` (detecção/substituição pura) + `lib/referencias.ts` (mapa vigente→proposta; ignora textos copiados do documento e o `texto_vigente` histórico) + `app/actions/referencias.ts` (atualização confirmada, nova versão/auditoria) + painel no dispositivo (`components/provision/reference-fixer.tsx`) e lista global em `/renumeracao` (`components/renumeracao/referencias-globais.tsx`). Consolidado (`/consolidado`) e exportações (`/api/export`) usam a **ordem/numeração da proposta** (aprovados; exclui revogados; inclui novos) e avisam quando a numeração diverge da ordem. Sem migração de banco.
- **Rascunho, Em construção e Comparativo** — a tela do dispositivo ganhou o **Rascunho comparativo** (aba Análise) em 3 colunas — **Vigente | Referência (proposta inicial) | Proposta (redação de trabalho)** — com toggle "Destacar diferenças" (diff por palavras via `lib/review-core.ts`; `components/provision/draft-comparison.tsx` + `components/review/compared-text.tsx` compartilhado). `/consolidado` virou **Estatuto em construção** (`?modo=construcao` padrão | `?modo=aprovados`): todos os dispositivos na ordem/numeração da proposta, selos de status/NOVO/revogado, chips `era X`, filtros (capítulo, status, busca, ocultar revogados, somente com texto) — `components/consolidado/estatuto-view.tsx`. Nova página `/comparativo` (**quadro final**): `Art. vigente → Art. proposta`, diff por palavras, tipo de alteração, justificativa, filtros e export .txt — `lib/comparativo-core.ts` (puro) + `components/comparativo/quadro-comparativo.tsx`. Painel: filtro "Somente com pendências" e atalho "Continuar de onde parou" (`getIdsComPendenciasAbertas`). Menu Reforma renomeado (Em construção / Comparativo). Sem migração de banco; testes Vitest 141.
- **Acompanhamento móvel dos membros** — `/comparativo` passa a se chamar **Acompanhamento** no menu e abre no modo dispositivo a dispositivo, com redação atual, vigente, proposta inicial, justificativa, pendências e mudança de numeração. O botão **Contribuir** reúne sugestão, comentário, pendência e anotação pessoal. O antigo quadro comparativo permanece como modo secundário na mesma rota. Na Mesa, a aba **Subsídios** passa a se chamar **Apoio à redação**. Sem migração de banco.
- **Navegação por perfil** — membros veem no fluxo da Reforma apenas **Acompanhamento** e **Proposta em construção**; coordenador e administrador também veem **Mesa de Trabalho** e o grupo de ferramentas (Pendências, Renumeração e Coerência). A rota legada `/revisao` permanece disponível por compatibilidade, mas saiu do menu. O menu móvel foi dividido por temas.
- **Ambiente de teste local (Docker)** — `node scripts/dev-db.mjs` sobe o Postgres `esdras-pg` (porta 5433); `node scripts/copiar-banco.mjs [--reset]` copia schema (`scripts/schema-postgres.sql`) + dados do Supabase para ele; `.env.development.local` (ignorado pelo git) aponta o `npm run dev` para esse banco e tem precedência sobre o `.env.local` — produção intacta. `lib/db.ts` desliga SSL em localhost.
- **Literatura de consulta (`/literatura`)** — biblioteca **no Postgres** (`library_books`/`library_sections`) de livros doutrinários, com leitura (sumário + busca por `busca` normalizada) e consulta IA. Diferente de `/documentos` (estática), aqui o admin adiciona livros **sem tocar em código**: `/admin/literatura` aceita **.md/.txt/.epub** (ou texto colado); o parse roda no navegador e a prévia é revisável (renomear/fundir/excluir seções) antes de salvar. Parser: `lib/literatura/parse.ts` (MD/TXT sujos — artefatos de OCR, títulos quebrados, seções pequenas/grandes) e `lib/literatura/epub.ts` (unzip com `fflate`, OPF/spine → XHTML → seções; sem DOM). Recuperação por IA: `lib/literatura/recuperacao.ts` + `lib/busca.ts` (pontuação compartilhada com as confissões); `action: consulta_doutrinaria` ganhou `fonte: livros|documentos|tudo` (toggle no formulário). Permissão `gerenciar_biblioteca` (admin). Conversão local de PDFs: `scripts/pdf-epub-para-md.py` (pdfplumber + pdftotext); carga inicial: `scripts/import-literatura.mjs` (lê `../Literatura de consulta/md-limpos`). Testes Vitest 120.
- **Mesa de Trabalho (`/mesa-trabalho`)** — ambiente integrado por capítulo: navegação e progresso à esquerda, documento em construção contínuo no centro e painel contextual à direita. Usa a árvore e a numeração reais da proposta; permite selecionar dispositivos sem trocar de página, comparar vigente × redação atual, consultar subsídios/status e incluir explicitamente capítulos, seções e subordinados. Redação (editor versionado, IA, justificativa e conclusão editorial explícita com confirmação/reabertura) e colaboração (sugestões, comentários, pendências e nota pessoal) abrem em diálogos dentro da Mesa. Concluir exige redação de trabalho não vazia, copia-a para `redacao_consolidada` e bloqueia edição até reabrir. A reorganização assistida (`lib/workbench-move.ts`) mostra destino/posição, prévia de todos os artigos renumerados e confirmação explícita; filhos seguem o dispositivo e a identidade permanece estável. A tela clássica permanece como consulta e contingência para histórico e recursos ainda não integrados, com retorno por query string. **Capítulos e seções são nós estruturais**: o conteúdo no documento é o título (editado por `updateProvisionTitle`), sem redação de corpo; a criação de nó estrutural exige título e dispensa texto; seção é renderizada como cabeçalho no Consolidado e na exportação. O painel contextual oferece **Excluir** (dispositivos sem correspondente no Estatuto registrado, com confirmação e aviso de exclusão em cascata dos filhos) e **Revogar/Desfazer revogação** (dispositivos do Estatuto registrado); a distinção vem do histórico (placement vigente ou `texto_vigente`), **não** da tag "novo". Após excluir, a Mesa seleciona o item anterior (`escolherSelecaoAposExclusao` em `lib/workbench-move.ts`).
- **Anotações de trabalho da Mesa (23/09/2026)** — a tag **"novo"** (`provisions.origem`) e a **origem referenciada** (`provisions.origem_ref_id` + `sem_origem`) passaram a ser escolha do operador (coordenador/admin), auditadas e sem alterar o histórico. A Mesa ganhou a aba **Origem** (painel contextual em 2×2: Comparação, Origem, Apoio à redação, Situação): um seletor com *Automático* (próprio número vigente), *Sem correspondente no Estatuto vigente* ou um dispositivo do Estatuto registrado. A referência define o chip **"era N"** (`resolverEra` em `lib/numeracao.ts`, consumido por `getNumerosArmazenados("vigente")`, exportações e navegação) e apresenta o texto vigente do dispositivo de origem com link. Ações `setTagNovo` e `setOrigemReferencia` em `app/actions/dispositivos.ts`; o formulário de novo dispositivo aceita origem opcional. A leitura comparativa (`lib/review-core.ts` e `quadro-comparativo.tsx`) usa a identidade histórica da importação, não a tag. Migração: `node scripts/migrate-origem-referencia.mjs`. Testes Vitest 162.
- **Renumeração local de dispositivos subordinados (23/09/2026)** — `lib/numeracao.ts:numerarSubordinados` deriva `§ 1º, § 2º…` (ou `Parágrafo único`), incisos romanos e alíneas alfabéticas da posição real de cada tipo entre irmãos na árvore da proposta; revogados não ocupam número. `getProposalTree` apresenta a numeração derivada e `getNumerosArmazenados("proposta")` retorna os números efetivamente exibidos, inclusive para movimentações anteriores ao ajuste. `moveProposalProvision` também persiste os números atualizados dos grupos de irmãos de origem e destino em `provision_placements`, preservando `provisions.numero` (vigente), e sinaliza referências de dispositivos concluídos cuja numeração mudou. A prévia `simulateArticleMove` foi ampliada para mostrar parágrafos, incisos e alíneas. **Não executar reescrita global retroativa do banco em leitura**; para movimentos antigos, a visualização é derivada, mas a persistência dos números locais só é atualizada quando o pai afetado participar de uma nova movimentação. Testes: `tests/numeracao.test.ts` e `tests/workbench-move.test.ts`.
- **Mesa de Trabalho 2.0 (entregue em 23/09/2026; migração de produção aplicada em 24/09/2026)** — Fases 1–7 do `../PRD_ESDRAS_MESA_TRABALHO_2_0.md` implementadas, homologadas pelo usuário e publicadas:
  - **Fase 1 — independência editorial**: `aprovado` não bloqueia mais edição (`updateRedacao`/`restoreRedacaoVersion`); `redacao_trabalho` é a fonte principal em Mesa, Consolidado, export e Coerência, e `redacao_consolidada` passa a ser marco histórico. Concordância em `provisions.acordo_version/acordo_em/acordo_por`, com aviso de alteração posterior e re-registro sem reabertura (`components/workbench/approval-control.tsx`).
  - **Fase 2 — exclusão reversível**: `deleteProvision` vira tombstone (`deleted_at`/`deleted_by`); `restoreProvision` devolve à posição original ou exige novo destino em conflito; `getProposalTree` poda subárvores retiradas (`podarExcluidos` em `lib/data.ts`); seção **Retirados da minuta** na Mesa (`getExcluidosProposta`).
  - **Fase 3 — minuta independente**: tipo `item` (LC 95) em CHECK/tipos/hierarquia/numeração/rótulos; `provision_correspondences` com vínculos múltiplos tipados e declarações de acréscimo/não aplicável (`lib/correspondencias.ts`, `app/actions/correspondencias.ts`, painel na aba Origem).
  - **Fase 4 — inserção/numeração**: `createProvision` aceita `afterId` (início/posição/fim) com renumeração dos irmãos; botões **Inserir antes/depois**; itens numerados por posição em `numerarSubordinados`.
  - **Fase 5 — editor híbrido/autosave**: edição direta no fluxo do capítulo (`WorkingTextEditor` inline) + modo artigo ampliado; `autosaveRedacao` grava a persistência corrente com debounce e fila serializada, sem versão por tecla; conflito com revisão manual (`sobrescreverRedacao`), buffer local em localStorage e indicador pendente/salvando/salvo/falha (`components/provision/working-text-editor.tsx`). Helpers puros em `lib/workbench-node.ts` e linha do documento em `components/workbench/document-row.tsx` (o workbench continua concentrando os diálogos).
  - **Fase 6 — marcos**: `document_snapshots` (JSONB) com criar/listar/restaurar/comparar; a restauração gera marco de segurança e retira (reversível) dispositivos criados depois; a comparação (`lib/marcos-core.ts` + `compararMarco`) lista criados, retirados, restaurados e alterações de texto/posição/status/correspondência (`app/actions/marcos.ts`, página `/marcos`).
  - **Fase 7 — conferência**: `lib/conferencia-core.ts` (pendências, numeração divergente, provisórios vazios, correspondências, remissões, justificativas, supressões) na página `/conferencia`, com **remissões livres vinculáveis a IDs estáveis** (`components/conferencia/remissao-links.tsx`, grava em `provision_relations`); exportação `?type=correspondencias` com acréscimos e supressões; Acompanhamento exibe vínculos e justificativas de escopo (`app/(app)/comparativo/page.tsx`).
  - Migração `scripts/migrate-mesa-2-0.mjs` (aditiva/idempotente, com `--local`) testada no clone com dados reais e aplicada em produção em 24/09/2026 (verificada sem perda de dados); `schema-postgres.sql`, `lib/schema.sql`, `migrate-to-pg.mjs` e `copiar-banco.mjs` em sincronia (o clone ignora tabelas ausentes na origem). Testes Vitest 186; build/lint verdes; smoke test autenticado no clone (Mesa, Conferência, Marcos e export). Diagnóstico, registro por fase, riscos e rollback em `docs/relatorio-mesa-2-0-2026-09-23.md`.
- **Acesso público ao PostgREST fechado (26/09/2026)** — `library_books`, `library_sections`, `provision_correspondences`, `document_snapshots`, `nova_mesa_drafts` e `nova_mesa_draft_versions` nasceram com RLS desligado e privilégios totais de `anon`/`authenticated` (default do Supabase), ficando legíveis/graváveis pelo PostgREST para quem tivesse a chave publishable (a mesma usada no Realtime, entregue ao navegador). Correção: `node scripts/migrate-rls-hardening.mjs [--local]` liga **RLS sem policies** (deny-all) nas seis, revoga tabelas/sequências de `anon`/`authenticated` e remove os default privileges para tabelas/sequências/funções futuras. A decisão de não migrar para Supabase Auth/RLS permanece; o backend (`lib/db.ts`, conexão como dono `postgres`) não é afetado. Aplicado em produção e verificado no mesmo dia: probe anônimo passou de HTTP 200 para 401 e os dados seguem íntegros (1 minuta, 15 versões, 6 livros, 405 seções).
- **Decisão vigente — aprovação e reuniões** — o sistema não realiza votação nem aprovação formal da proposta. O status técnico legado `aprovado` é exibido como **Redação concluída** e significa apenas conclusão editorial. A aprovação formal ocorre pela assinatura da comissão no documento final encaminhado à assembleia. Reuniões servem para presença, registros manuais e eventual ata; deliberações antigas ficam somente como histórico legado e não entram em novas atas.

## Comandos

```bash
npm run dev        # servidor de desenvolvimento (http://localhost:3000) — usa DATABASE_URL (Supabase)
npm run build      # build de produção (inclui type check)
npm run lint       # eslint
npm test           # vitest (módulos puros)
node scripts/migrate-to-pg.mjs   # PORTA schema+dados do SQLite local para o Supabase (drop+recria)
node scripts/migrate-users-phone-password.mjs   # adiciona users.phone/must_change_password no Postgres existente
node scripts/migrate-personal-notes.mjs         # cria a tabela personal_notes no Postgres existente
node scripts/migrate-literature.mjs             # cria library_books/library_sections no Postgres existente
node scripts/migrate-origem-referencia.mjs      # adiciona provisions.origem_ref_id/sem_origem no Postgres existente
node scripts/migrate-mesa-2-0.mjs [--local]     # Mesa 2.0: exclusão reversível, correspondências múltiplas, marcos e tipo item
node scripts/migrate-rls-hardening.mjs [--local] # RLS deny-all + revoga anon/authenticated das tabelas expostas ao PostgREST
node scripts/import-literatura.mjs              # importa os MDs de ../Literatura de consulta/md-limpos (idempotente por título)
node scripts/dev-db.mjs                         # sobe o Postgres de teste local (Docker, porta 5433)
node scripts/copiar-banco.mjs [--reset]         # copia schema+dados do Supabase para o banco de teste local
python scripts/pdf-epub-para-md.py              # converte PDFs/EPUBs de ../Literatura de consulta em MD limpo (md-limpos/)
node scripts/build-confissoes.mjs               # regenera lib/confissoes/data/*.json a partir de ../Documentos fonte
node scripts/generate-icons.mjs                 # regenera os ícones PNG do PWA em public/icons (usa sharp)
npm run seed       # (só SQLite local/dev) recria data/esdras.db
node scripts/demo-meeting.mjs    # (opcional) recria a reunião demo nº 1
```

> O `npm run seed` recria o SQLite local (`data/esdras.db`) — usado como fonte para a migração; o app em execução usa o Postgres do Supabase.

## Arquitetura e convenções

- **Next.js 16** (App Router, Turbopack, RSC). Consulte os docs em `node_modules/next/dist/docs/` antes de escrever código (ex.: `proxy.ts` substitui `middleware.ts`; `params` é `Promise`; layout usa `LayoutProps`).
- **Páginas**: `app/(app)/` (área autenticada). Rotas com dados dinâmicos usam `export const dynamic = "force-dynamic"`.
- **Mutações**: Server Actions em `app/actions/*.ts` — cada ação valida perfil no servidor (`requireRole`/`requireUser` de `lib/auth.ts`) e registra em `audit_logs`. Nunca confiar só no frontend.
- **Banco**: `lib/db.ts` (pg async: `get/all/run/transaction`). Queries usam `?` (convertido em runtime). `run()` retorna `lastInsertRowid` via `RETURNING id`. IDs dos dispositivos são slugs estáveis (`art-1`, `art-4-p2`, `cap-2`); novos usam `novo-<timestamp36>-<rand>`. Datas: string `YYYY-MM-DD HH:MM:SS` (evitar `now()` do Postgres direto — usar `now()` de `lib/db.ts` ou `datetime('now')` que é convertido).
- **Perfis**: centralizados em `lib/permissions.ts` (módulo puro `PERMISSOES`/`rolesCom`/`temPermissao`) — os server actions usam `requireRole(...rolesCom("..."))`, nunca listas de roles hardcoded. `admin` (tudo, corrige extração, auditoria/admin só dele), `coordenador` (redação de trabalho, status, reuniões, incluir/editar/excluir dispositivos não-originais, mover, renumeração), `membro` (contribuir: sugestões, comentários, pendências, referências, votos; e revisar atas). Auditoria e Administração visíveis **somente para admin**. Controle de concorrência da redação (§41) via `lib/version-guard.ts` (`avaliarConflito`).
- **Regras de negócio centrais** (PRD §50): integridade dos dados → histórico → simplicidade → colaboração → estética. Nenhuma contribuição sobrescreve outra; toda alteração da redação de trabalho cria versão; decisão sempre vinculada à reunião e ao dispositivo.
- **Modelos de IA**: o `llama-3.3-70b-versatile` foi **descontinuado na Groq (16/08/2026)**. O `app/api/ai/route.ts` usa cadeia de fallback: `GROQ_MODEL` → `openai/gpt-oss-120b` → `qwen/qwen3.6-27b` → `openai/gpt-oss-20b`. A chave fica em `.env.local` (`GROQ_API_KEY`), nunca no frontend. **Orçamento de tokens** (`lib/ai-budget.ts`): o plano gratuito limita ~8.000 tokens/minuto (TPM); a rota estima (~3,5 chars/token), limita a entrada (`GROQ_MAX_INPUT_TOKENS`, padrão 4600), limita a saída (`GROQ_MAX_OUTPUT_TOKENS`, padrão 2048) e, em erro 413/429 (TPM), **reduz o contexto** (100% → 60% → 35%) e tenta o próximo modelo; se ainda falhar, devolve mensagem orientando a refinar a pergunta. `gpt-oss` usa `reasoning_effort: "low"`. Consultas doutrinárias usam 5 trechos × 2500 chars (3 × 2200 no modo "tudo"); a ajuda usa `formarContextoAjuda(pergunta)` (seções relevantes do manual).
- **Ordenação da árvore**: `ordem_pai` (posição entre irmãos); `ordem` é a sequência global. Exportações seguem `ordem_pai`.

## Setup em máquina nova

```bash
npm install
cp .env.example .env.local   # ajustar SESSION_SECRET (e GROQ_API_KEY quando tiver)
npm run seed
npm run dev
```

## Estrutura relevante

- `app/(app)/dispositivo/[id]/page.tsx` — tela de análise (área central do sistema)
- `components/provision/*` — editor de redação, sugestões, comentários, pendências, ações do dispositivo
- `components/meetings/*` — CRUD de reuniões, presença, registros manuais e ata
- `components/confirm-dialog.tsx` — modal de confirmação reutilizável
- `lib/seed-data/*.json` — Estatuto registrado estruturado por capítulo (fonte dos dados)
- `lib/legal-refs.ts` — guia curado de técnica legislativa (LC 95/1998) e redação oficial (MRPR); usado nos prompts de IA e na página `/guia-redacao` (`components/guia/*`)
- `lib/confissoes/` — biblioteca doutrinária: `data/*.json` (textos gerados por `scripts/build-confissoes.mjs`), módulos por documento (metadados), `recuperacao.ts` (busca por trechos para IA); página `/documentos` (`components/documentos/*`)
- `lib/literatura/` — biblioteca de livros (Postgres): `parse.ts` (MD/TXT → seções), `epub.ts` (EPUB → seções), `recuperacao.ts` (trechos para IA), `types.ts`; `lib/busca.ts` — normalização/tokenização/pontuação compartilhadas; páginas `/literatura` e `/literatura/[bookId]`; admin em `/admin/literatura` (`components/admin/library-admin.tsx`)
- `lib/numeracao.ts` — numeração derivada (artigos/capítulos, revogados fora); `lib/versao.ts` — cookie da versão de trabalho; `lib/referencias-core.ts` + `lib/referencias.ts` + `app/actions/referencias.ts` — referências internas assistidas; `components/provision/reference-fixer.tsx` e `components/renumeracao/referencias-globais.tsx`
- `scripts/schema-postgres.sql` — DDL Postgres (usado por `copiar-banco.mjs`); `.env.development.local` — banco de teste local (não versionado)
- `lib/manual.ts` — manual de utilização (16 seções, puro); página `/manual` (`components/manual/*`), onboarding (`components/onboarding/*`, estado em localStorage) e ajuda (`components/help/*`, `action: ajuda` no `/api/ai`)
- `app/manifest.ts` + `public/sw.js` + `public/icons/*` — PWA (manifest, service worker e ícones; `scripts/generate-icons.mjs` regenera os PNG)
- `scripts/seed.mjs` — cria o banco a partir dos JSONs
